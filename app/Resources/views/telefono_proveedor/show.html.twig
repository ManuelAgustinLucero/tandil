{% extends 'base.html.twig' %}

{% block body %}
    <h1>Telefono_proveedor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ telefono_Proveedor.id }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ telefono_Proveedor.telefono }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_telefono_proveedor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_telefono_proveedor_edit', { 'id': telefono_Proveedor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
