{% extends 'base.html.twig' %}

{% block body %}
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-8 col-md-7 col-lg-offset-2">
                    <div class="card">
                        <div class="header">
                            <h4 class="title text-center">Crear Proveedor (carga de telefonos)</h4>
                            {% for flash_message in app.session.flashbag.get('notice') %}
                                <div class="flash-notice text-success text-center ">
                                    <h4>{{ flash_message }}</h4>
                                </div>
                            {% endfor %}
                        </div>
                        <div class="content">
                            {{ form_start(form) }}
                            <div class="row">
                                <div class="col-md-6 col-lg-offset-3" >
                                    <div class="form-group" >
                                        <label>Telefono</label>
                                        {{ form_widget(form.telefono, { 'attr': {'class':'form-control border-input'}})}}
                                    </div>
                                </div>
                            </div>
                            <div class="row hide">
                                <div class="col-md-6 col-lg-offset-3" >
                                    <div class="form-group" >
                                        <label></label>
                                        {{ form_widget(form.proveedor, { 'attr': {'class':'form-control border-input'}})}}
                                    </div>
                                </div>
                            </div>

                            <div class="text-center">
                                <a  class="btn btn-info btn-fill btn-wd"  href="{{ path('admin_proveedor_index') }}">Volver a la lista</a>

                                <input class="btn btn-success btn-fill btn-wd" type="submit" value="Agregar" />
                            </div>
                            <div class="clearfix"></div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
